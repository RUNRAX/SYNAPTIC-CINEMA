app = "synaptic-cinema-backend"
primary_region = "iad"

[build]

[deploy]
  release_command = "pip install -r requirements.txt"

[run]
  port = 8080
  protocol = "tcp"

[env]
  PORT = "8080"
  FLASK_ENV = "production"
  PYTHONUNBUFFERED = "true"
