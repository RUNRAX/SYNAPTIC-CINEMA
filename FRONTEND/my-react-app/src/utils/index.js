// src/utils/index.js

/**
 * Creates a consistent URL path for a given page name.
 * This function is used by components to navigate within the app.
 */
export const createPageUrl = (page) => `/${page}`;